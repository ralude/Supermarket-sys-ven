import { useEffect, useState } from 'react';
import type { CategoryResponse, ProductResponse, UnitOfMeasureResponse } from '@supermarket/shared';
import { createProductContract, isPermissionGranted, updatePriceContract } from '@supermarket/shared';
import { createIdempotencyKey, formatScaledDecimal, parseMinorUnits } from '../api-client.js';
import { ActionButton, EmptyState, Feedback, ScreenNote, money, type ScreenProps } from './shared.js';

/** Escala de moneda asumida para el precio, igual que el resto de la aplicación (Caja incluida). */
const PRICE_SCALE = 2;

export const CatalogScreen = ({ api, permissionCodes }: ScreenProps): React.JSX.Element => {
  const [barcode, setBarcode] = useState(''); const [product, setProduct] = useState<ProductResponse | null>(null); const [products, setProducts] = useState<readonly ProductResponse[]>([]); const [history, setHistory] = useState<readonly { id: string; priceMinorUnits: number; currencyCode: string; recordedAt: string; recordedBy: string; reason: string }[]>([]); const [price, setPrice] = useState(''); const [reason, setReason] = useState(''); const [error, setError] = useState<unknown>(null); const [notice, setNotice] = useState<string | null>(null); const [loading, setLoading] = useState(false); const [showCreate, setShowCreate] = useState(false); const [name, setName] = useState(''); const [description, setDescription] = useState(''); const [categoryId, setCategoryId] = useState(''); const [unitCode, setUnitCode] = useState(''); const [newBarcode, setNewBarcode] = useState(''); const [newPrice, setNewPrice] = useState(''); const [newCurrency, setNewCurrency] = useState('USD'); const [taxRate, setTaxRate] = useState('0');
  const [categories, setCategories] = useState<readonly CategoryResponse[]>([]);
  const [units, setUnits] = useState<readonly UnitOfMeasureResponse[]>([]);
  useEffect(() => {
    void api.listCategories().then(setCategories).catch(() => undefined);
    void api.listUnitsOfMeasure().then(setUnits).catch(() => undefined);
  }, [api]);
  const dismissFeedback = (): void => { setError(null); setNotice(null); };
  const list = async (): Promise<void> => { setLoading(true); setError(null); try { setProducts(await api.listProducts(barcode)); } catch (nextError) { setError(nextError); } finally { setLoading(false); } };
  const search = async (event: React.FormEvent<HTMLFormElement>): Promise<void> => { event.preventDefault(); setLoading(true); setError(null); setNotice(null); try { const response = await api.findProductByBarcode(barcode.trim()); setProduct(response.product); setPrice(formatScaledDecimal(response.product.price.amountMinorUnits, PRICE_SCALE)); setHistory(await api.getPriceHistory(response.product.id)); } catch (nextError) { setProduct(null); setError(nextError); } finally { setLoading(false); } };
  const savePrice = async (event: React.FormEvent<HTMLFormElement>): Promise<void> => {
    event.preventDefault(); if (!product) return; setLoading(true); setError(null);
    try {
      const priceMinorUnits = parseMinorUnits(price, PRICE_SCALE);
      setProduct(await api.updatePrice(product.id, { priceMinorUnits, currencyCode: product.price.currencyCode, reason: reason.trim() }, createIdempotencyKey()));
      setNotice('Precio actualizado.');
    } catch (nextError) { setError(nextError); } finally { setLoading(false); }
  };
  const create = async (event: React.FormEvent<HTMLFormElement>): Promise<void> => {
    event.preventDefault(); setLoading(true); setError(null);
    try {
      const priceMinorUnits = parseMinorUnits(newPrice, PRICE_SCALE);
      const created = await api.createProduct({ name: name.trim(), description: description.trim(), categoryId, unitCode, barcodes: [newBarcode.trim()], priceMinorUnits, currencyCode: newCurrency.trim().toUpperCase(), taxRateBasisPoints: Number(taxRate), reason: reason.trim() }, createIdempotencyKey());
      setProduct(created); setShowCreate(false); setNotice('Producto creado.');
    } catch (nextError) { setError(nextError); } finally { setLoading(false); }
  };
  return <div className="operation-screen"><ScreenNote>Busca productos por barcode y muestra el snapshot que devuelve el nodo. Los cambios de precio quedan auditados.</ScreenNote><Feedback error={error} notice={notice} onDismiss={dismissFeedback} /><section className="panel"><div className="screen-toolbar"><form className="inline-form grow" onSubmit={search}><label className="grow">Barcode<input value={barcode} onChange={(event) => setBarcode(event.target.value)} autoFocus /></label><ActionButton className="primary-button" type="submit" busy={loading} disabled={loading}>{loading ? 'Buscando…' : 'Buscar barcode'}</ActionButton></form><ActionButton type="button" onClick={() => void list()} busy={loading} disabled={loading}>Cargar listado</ActionButton><button type="button" onClick={() => setShowCreate((value) => !value)}>{showCreate ? 'Cancelar' : 'Nuevo producto'}</button></div>{showCreate && <form className="stack-form" onSubmit={create}><div className="form-grid"><label>Nombre<input value={name} onChange={(event) => setName(event.target.value)} required /></label><label>Descripción<input value={description} onChange={(event) => setDescription(event.target.value)} required /></label><label>Categoría<select value={categoryId} onChange={(event) => setCategoryId(event.target.value)} required><option value="">Selecciona</option>{categories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</select></label><label>Unidad<select value={unitCode} onChange={(event) => setUnitCode(event.target.value)} required><option value="">Selecciona</option>{units.map((unit) => <option key={unit.code} value={unit.code}>{unit.name}</option>)}</select></label><label>Barcode<input value={newBarcode} onChange={(event) => setNewBarcode(event.target.value)} required /></label><label>Precio<input inputMode="decimal" value={newPrice} onChange={(event) => setNewPrice(event.target.value)} placeholder="0,00" required /></label><label>Moneda<input value={newCurrency} onChange={(event) => setNewCurrency(event.target.value.toUpperCase())} required /></label><label>IVA (puntos base)<input type="number" min="0" value={taxRate} onChange={(event) => setTaxRate(event.target.value)} required /></label></div><label>Motivo<input value={reason} onChange={(event) => setReason(event.target.value)} required /></label><ActionButton className="primary-button" type="submit" busy={loading} disabled={loading || !categoryId || !unitCode || !isPermissionGranted(createProductContract.permission, permissionCodes)}>{loading ? 'Creando…' : 'Crear producto'}</ActionButton></form>}</section>{products.length > 0 && <section className="panel"><p className="eyebrow">Listado consultado</p><div className="table-wrap"><table><thead><tr><th>Producto</th><th>Barcode</th><th>Precio</th><th /></tr></thead><tbody>{products.map((item) => <tr key={item.id}><td>{item.name}</td><td>{item.barcodes.join(', ')}</td><td>{money(item.price.amountMinorUnits, item.price.currencyCode)}</td><td><button type="button" onClick={() => { setProduct(item); setPrice(formatScaledDecimal(item.price.amountMinorUnits, PRICE_SCALE)); void api.getPriceHistory(item.id).then(setHistory).catch(setError); }}>Ver ficha</button></td></tr>)}</tbody></table></div></section>}{product ? <section className="panel product-detail"><div><p className="eyebrow">Producto encontrado</p><h3>{product.name}</h3><p className="muted">{product.description}</p><dl className="detail-grid"><div><dt>Unidad</dt><dd>{product.unitCode}</dd></div><div><dt>Barcode</dt><dd>{product.barcodes.join(', ')}</dd></div><div><dt>Estado</dt><dd>{product.isActive ? 'Activo' : 'Inactivo'}</dd></div><div><dt>Versión</dt><dd>{product.version}</dd></div></dl><h4>Historial de precio</h4>{history.length === 0 ? <p className="muted">Sin registros.</p> : <div className="table-wrap"><table><thead><tr><th>Fecha</th><th>Precio</th><th>Motivo</th></tr></thead><tbody>{history.map((entry) => <tr key={entry.id}><td>{new Date(entry.recordedAt).toLocaleString('es-VE')}</td><td>{money(entry.priceMinorUnits, entry.currencyCode)}</td><td>{entry.reason}</td></tr>)}</tbody></table></div>}</div><form className="stack-form" onSubmit={savePrice}><label>Precio<input inputMode="decimal" value={price} onChange={(event) => setPrice(event.target.value)} placeholder="0,00" required /></label><label>Motivo<input value={reason} onChange={(event) => setReason(event.target.value)} required /></label><ActionButton className="primary-button" type="submit" busy={loading} disabled={loading || !isPermissionGranted(updatePriceContract.permission, permissionCodes)}>{loading ? 'Actualizando…' : 'Actualizar precio'}</ActionButton></form></section> : <EmptyState>Busca un barcode o carga el listado para consultar el catálogo.</EmptyState>}</div>;
};
